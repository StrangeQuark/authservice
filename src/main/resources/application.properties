# Application name
spring.application.name=${SPRING_APPLICATION_NAME:authservice}

# Server port
server.port=${SERVER_PORT:6001}

# Logging
logging.level.com.strangequark=${LOGGING_LEVEL:INFO}
service.http.connect.timeout=${SERVICE_HTTP_CONNECT_TIMEOUT:5000}
service.http.read.timeout=${SERVICE_HTTP_READ_TIMEOUT:10000}

# DB config
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}

# Hibernate/JPA
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL:true}
spring.jpa.database=${SPRING_JPA_DATABASE:postgresql}
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}

# CORS origins
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:6080}

# JWT keys
JWT_PRIVATE_KEY=${JWT_PRIVATE_KEY}
JWT_PUBLIC_KEY=${JWT_PUBLIC_KEY}
JWT_ISSUER=${JWT_ISSUER}
ENCRYPTION_KEY=${ENCRYPTION_KEY}
SERVICE_SECRET_AUTH=${SERVICE_SECRET_AUTH}
INITIAL_SUPER_CREDENTIALS_FILE=${INITIAL_SUPER_CREDENTIALS_FILE}
INITIAL_SUPER_USERNAME=${INITIAL_SUPER_USERNAME:}
INITIAL_SUPER_PASSWORD=${INITIAL_SUPER_PASSWORD:}
cookie.secure=${COOKIE_SECURE:true}
